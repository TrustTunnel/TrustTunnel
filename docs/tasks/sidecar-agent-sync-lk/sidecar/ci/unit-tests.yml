name: Sidecar unit tests
on: [push]
jobs:
  unit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: |
          docker build -t sidecar:ci -f sidecar/Dockerfile .
      - name: Run placeholder tests
        run: |
          echo "TODO: add go test"
