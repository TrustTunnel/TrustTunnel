# REPORTS — прогресс по задачам sidecar-agent

> В этот файл добавляются отчеты **по каждой задаче из `README.md`** по мере выполнения.

## Шаблон записи
- Дата/время:
- Задача: (например, `3) Injection`)
- Статус: (`in_progress` | `blocked` | `done`)
- Что сделано:
- Что осталось:
- Блокеры/риски:
- Ссылки: (PR/commit/issue/runbook)

---

## Журнал
<!-- Добавляйте новые записи сверху -->

- Дата/время: 2026-03-04 00:00 UTC
- Задача: `0) Ключевое ограничение (ОБЯЗАТЕЛЬНО)`
- Статус: `in_progress`
- Что сделано:
  - Зафиксированы рамки реализации: sidecar должен оставаться pod-local и не использовать `hostPort/hostNetwork`.
  - Проведена ревизия текущего `sidecar-agent`: логика работы строится на синке с LK и локальном обновлении credentials-файла, без бинда на `443`.
- Что осталось:
  - Добавить/подтвердить это ограничение в deployment/injector-манифестах для sidecar-сценария в LK интеграции.
- Блокеры/риски:
  - В репозитории пока нет отдельного injector/webhook артефакта для автоматической инъекции sidecar.
- Ссылки: commit (будет добавлен после фиксации пакета задач)

- Дата/время: 2026-03-04 00:00 UTC
- Задача: `1) Назначение sidecar`
- Статус: `in_progress`
- Что сделано:
  - Подтверждена текущая функциональность sidecar: синк snapshot из LK, retry/backoff, sync-report и метрики/health.
  - Есть dedup по `version/checksum` и heartbeat-подобный health/metrics цикл.
- Что осталось:
  - Добавить pod-контекст (`pod_name`, `ns`, `uid`, `node_name`, `pod_ip`) в payload-ы.
  - Реализовать file-based watch для ConfigMap/Secret mount-ов и отдельный протокол отправки config/secret объектов.
- Блокеры/риски:
  - Протокол в коде и ТЗ в задаче расходятся (сейчас credentials snapshot, а не config/secret sync).
- Ссылки: commit (будет добавлен после фиксации пакета задач)

- Дата/время: 2026-03-04 00:00 UTC
- Задача: `2) Как sidecar получает ConfigMap/Secret (вариант A = базовый)`
- Статус: `blocked`
- Что сделано:
  - Уточнены требуемые mount-path (`/etc/trusttunnel/configs`, `/etc/trusttunnel/secrets`) и модель отслеживания через `fsnotify`.
- Что осталось:
  - Реализация injector/mount wiring и watcher-цикла в sidecar.
- Блокеры/риски:
  - Нет готового injector-компонента; требуется отдельный этап проектирования и внедрения.
- Ссылки: commit (будет добавлен после фиксации пакета задач)

- Дата/время: 2026-03-04 00:00 UTC
- Задача: `3) Injection (автоматическое добавление sidecar)`
- Статус: `blocked`
- Что сделано:
  - Зафиксирован перечень аннотаций и обязательных env для injector поведения.
- Что осталось:
  - Реализовать MutatingAdmissionWebhook либо временный Helm patching-path.
  - Добавить acceptance-проверку автоподмешивания sidecar.
- Блокеры/риски:
  - В текущем репозитории отсутствует готовый webhook service/controller для sidecar injection.
- Ссылки: commit (будет добавлен после фиксации пакета задач)

- Дата/время: 2026-03-04 00:00 UTC
- Задача: `4) Протокол с LK`
- Статус: `in_progress`
- Что сделано:
  - Подтверждена частичная готовность: sidecar уже общается с LK по HTTPS и отправляет sync-report + метрики.
- Что осталось:
  - Добавить lifecycle: register (`node_id/node_token`), config payload endpoint, heartbeat endpoint.
  - Добавить контракт для secrets metadata и opt-in encrypted keys.
- Блокеры/риски:
  - Требуются новые endpoint-ы в LK или актуализация контракта существующих внутренних endpoint-ов.
- Ссылки: commit (будет добавлен после фиксации пакета задач)

- Дата/время: 2026-03-04 00:00 UTC
- Задача: `5) Безопасность`
- Статус: `in_progress`
- Что сделано:
  - HTTPS и bearer auth уже используются в HTTP-клиенте sidecar.
- Что осталось:
  - Перейти на короткоживущий `node_token` (JWT lifecycle).
  - Добавить шифрование разрешенных secret keys (`RSA/OAEP` или `age`).
  - Оформить NetworkPolicy для egress только к LK endpoint.
- Блокеры/риски:
  - Не выбран и не зафиксирован единый crypto-формат для секрета.
- Ссылки: commit (будет добавлен после фиксации пакета задач)

- Дата/время: 2026-03-04 00:00 UTC
- Задача: `6) Надежность`
- Статус: `in_progress`
- Что сделано:
  - Есть exponential backoff при получении snapshot.
  - Есть dedup по `version/checksum`.
- Что осталось:
  - Добавить disk/memory cache последних изменений для replay при недоступности LK в новой модели sync config/secret.
- Блокеры/риски:
  - Семантика кэша зависит от финального протокола разделения config и secret payload.
- Ссылки: commit (будет добавлен после фиксации пакета задач)

- Дата/время: 2026-03-04 00:00 UTC
- Задача: `7) Observability`
- Статус: `in_progress`
- Что сделано:
  - Логи и отправка метрик уже есть (sync ошибки/успех, latency контролируется статус-кодами и интервалами).
- Что осталось:
  - Добавить отдельные счетчики `sync_errors_total`, `last_sync_ts` в совместимом с LK формате.
- Блокеры/риски:
  - Потребуется синхронизация формата метрик между sidecar и LK backend.
- Ссылки: commit (будет добавлен после фиксации пакета задач)

- Дата/время: 2026-03-04 00:00 UTC
- Задача: `8) Definition of Done (TrustTunnel)`
- Статус: `in_progress`
- Что сделано:
  - Подготовлен baseline-отчет о текущем покрытии DoD относительно текущей реализации.
- Что осталось:
  - Закрыть пункты по injection, configmap/secret sync <=30s и secret encryption policy.
- Блокеры/риски:
  - Без реализации задач 2-4 и части 5 DoD полностью не закрывается.
- Ссылки: commit (будет добавлен после фиксации пакета задач)
